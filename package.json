{
  "name": "back-end",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "nodemon -r dotenv/config server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon -r dotenv/config server.js",
    "db-reset": "node -r dotenv/config db/reset-tables.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.8.0"
  },
  "devDependencies": {
    "cypress": "^11.2.0",
    "dotenv": "^16.0.3",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.20"
  }
}
